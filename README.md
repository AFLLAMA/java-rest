# To run the project
## 1. Setup JAVA_HOME env variable
## 2. To compile run
$JAVA_HOME/bin/javac -d out src/com/example/rest/*.java
## 3. To start server in background
$JAVA_HOME/bin/java -cp out com.example.rest.App &
